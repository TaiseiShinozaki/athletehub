<!DOCTYPE html>
<html>
  <head>
    <meta charaset="UTF-8">
    <link rel="stylesheet" href="index.css">
  </head>
  <body>
    <div class="contents">
      <h1 class="heading-name">投稿一覧</h1>
      <ul class="content-lists">
        <% @contents.each do |content| %>
        <li><%= link_to content.title, content_path(content.id), id: "content-title" %></li>
        <% end %>
      </ul>
    </div>

  </body>
</html>
